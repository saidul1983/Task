@model IList<CategoryModel>

@using Nop.Core.Domain.Catalog

@if (Model.Count > 0)
{
    @* <h2 class="text-uppercase mb-3"> Top Sales </h2>
    <div class="products-slider">
        <div class="owl-carousel owl-theme owl-loaded owl-drag" id="top-sales">

            <div class="item">
                @foreach (var item in Model)
                {
                    <article class="products-box">
                        <h2 class="title">
                            <a href="@(await NopUrl.RouteGenericUrlAsync<Category>(new { SeName = item.SeName }))" title="@item.PictureModel.Title">
                                @item.Name
                            </a>
                        </h2>
                        <div class="picture">
                            <a href="@(await NopUrl.RouteGenericUrlAsync<Category>(new { SeName = item.SeName }))" title="@item.PictureModel.Title">
                                <img alt="@item.PictureModel.AlternateText" src="@item.PictureModel.ImageUrl" title="@item.PictureModel.Title" />
                            </a>
                        </div>
                    </article>
                }
            </div>
        </div>
    </div> *@
}